{
  "name": "autodealercloud",
  "version": "1.0.0",
  "description": "Multi-tenant SaaS CMS for auto dealerships",
  "private": true,
  "scripts": {
    "dev": "docker-compose up",
    "build": "docker-compose build",
    "down": "docker-compose down",
    "logs": "docker-compose logs -f"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
