import tsconfig from '../../tsconfig.json'

export = {
  extends: tsconfig,
  compilerOptions: {
    outDir: './dist',
    module: 'ESNext',
    target: 'ES2020',
  },
  include: ['src/**/*'],
  exclude: ['node_modules'],
}
